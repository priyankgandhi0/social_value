import 'package:flutter/material.dart';

class MentalHealthArticles extends StatelessWidget {
  const MentalHealthArticles({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}
