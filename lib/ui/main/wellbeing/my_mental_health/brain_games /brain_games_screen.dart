import 'package:flutter/material.dart';

class MentalHealthBrainGames extends StatelessWidget {
  const MentalHealthBrainGames({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}
