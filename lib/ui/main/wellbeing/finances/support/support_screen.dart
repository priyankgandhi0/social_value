import 'package:flutter/material.dart';

class FinancesSupport extends StatelessWidget {
  const FinancesSupport({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}
